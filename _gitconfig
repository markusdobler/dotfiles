[user]
	email = mail@markusdobler.de
	name = Markus Dobler

[push]
	default = simple

[core]
    pager = less -FRSX
    editor = vim

[merge]
    tool = threesome

[mergetool "threesome"]
    cmd = "gvim -f $BASE $LOCAL $REMOTE $MERGED -c 'ThreesomeInit'"
    trustExitCode = true
