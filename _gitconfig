[user]
	email = mail@markusdobler.de
	name = Markus Dobler

[push]
	default = simple

[core]
    pager = less -FRSX
    editor = vim


[merge]
    tool = splice

[mergetool "splice"]
    cmd = "gvim -f $BASE $LOCAL $REMOTE $MERGED -c 'SpliceInit'"
    trustExitCode = true
